<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="brunogabriel.github.io.androidrealmnotes">

    <application
        android:allowBackup="true"
        android:name=".manager.App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ApplicationTheme">

        <activity android:name=".views.activities.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ApplicationTheme.NotTranslucent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".views.activities.SignupActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ApplicationTheme.NotTranslucent">
        </activity>

    </application>

</manifest>
